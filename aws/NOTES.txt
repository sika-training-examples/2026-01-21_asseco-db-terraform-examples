# Install AWS CLI

```
mkdir -p /tmp/install-aws-cli
cd /tmp/install-aws-cli
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install
rm -rf /tmp/install-aws-cli
```
